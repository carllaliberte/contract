name: Orchestrate CreatorFlow

on:
  workflow_dispatch:
    inputs:
      pipeline:
        description: Pipeline to run
        type: choice
        options:
          - ci
          - deploy
          - ci-and-deploy
        default: ci-and-deploy
      ref:
        description: Branch, tag, or SHA
        required: true
        default: main

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch CI CreatorFlow
        if: inputs.pipeline == 'ci' || inputs.pipeline == 'ci-and-deploy'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'ci-creatorflow.yml',
              ref: '${{ inputs.ref }}',
            });
            core.info(`Dispatched CI CreatorFlow on ref ${'${{ inputs.ref }}'}`);

      - name: Dispatch Deploy CreatorFlow
        if: inputs.pipeline == 'deploy'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'deploy-creatorflow.yml',
              ref: '${{ inputs.ref }}',
            });
            core.info(`Dispatched Deploy CreatorFlow on ref ${'${{ inputs.ref }}'}`);

      - name: ci-and-deploy note
        if: inputs.pipeline == 'ci-and-deploy'
        run: |
          echo "CI CreatorFlow dispatched on ref ${{ inputs.ref }}."
          echo "Deploy CreatorFlow will run automatically after CI succeeds on main (workflow_run)."
