{
  "name": "google-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "compile-contract": "node scripts/compile-contract.mjs"
  },
  "devDependencies": {
    "solc": "^0.6.12",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "ethers": "^6.17.0"
  }
}
